name: Breaking news post

on:
  schedule:
    # 4 時間ごと (JST 06:00, 10:00, 14:00, 18:00, 22:00 / UTC 21:00, 01:00, 05:00, 09:00, 13:00)
    - cron: "0 21,1,5,9,13 * * *"
  workflow_dispatch:
    inputs:
      threshold:
        description: "重要度スコアの投稿閾値 (0-10)"
        required: false
        default: "6"
      max_items:
        description: "最大投稿数"
        required: false
        default: "3"
      dry_run:
        description: "dry-run (Discord に投稿しない)"
        type: boolean
        required: false
        default: false
      skip_dedupe:
        description: "state.json を無視 (重複投稿する)"
        type: boolean
        required: false
        default: false

concurrency:
  group: news-bot-post
  cancel-in-progress: false

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Restore posted state
        uses: actions/cache@v4
        with:
          path: state/posted.json
          key: news-bot-posted-${{ github.run_id }}
          restore-keys: |
            news-bot-posted-

      - name: Run news bot
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          IMPORTANCE_THRESHOLD: ${{ github.event.inputs.threshold || '6' }}
          MAX_ITEMS_PER_RUN: ${{ github.event.inputs.max_items || '3' }}
          LOG_LEVEL: INFO
        run: |
          FLAGS=""
          [ "${{ github.event.inputs.dry_run }}" = "true" ] && FLAGS="$FLAGS --dry-run"
          [ "${{ github.event.inputs.skip_dedupe }}" = "true" ] && FLAGS="$FLAGS --skip-dedupe"
          python -m src.main $FLAGS
